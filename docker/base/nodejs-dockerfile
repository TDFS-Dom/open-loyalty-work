# Based on Official Node.js Docker image
FROM node:8.17.0
# Install `sassc` gem
RUN apt-get update -qq && apt-get install -y build-essential ruby ruby-dev
RUN gem install sassc

# Install gulp and node-sass globally
RUN npm install --unsafe-perm -g gulp node-sass
